# DooDrop

Starter scaffold for DooDrop (Flask backend + Vue 3 frontend)

## Local dev

1. Install Docker & Docker Compose
2. Copy backend/.env.example -> backend/.env and set values
3. docker-compose up --build
4. Frontend at http://localhost:3000, backend API at http://localhost:5000